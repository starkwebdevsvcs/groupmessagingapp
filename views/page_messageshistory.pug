extends page_layout2

block content
    if user
        .container
            h1 #{title}
            br
            hr
            br
            h4 #{title2}
            table#historyTable.table.table-striped.table-bordered(style='width:100%')
                thead
                    tr
                        th(scope='col') Date/Time:
                        th(scope='col') Sent To:
                        th(scope='col') Text Message:
                        th(scope='col') Status:
                        //- th(scope='col') SID:                        
                tbody
                    each message, i in sentMessages
                        tr
                            td= moment(message.dateSent).format("MM/DD/YY, HH:mm")
                            td= message.to
                            td= message.body
                            td= message.status
                            //- td
                            //-     a.delete-msg(href='#' data-sid=response.sid title='Click to Delete Message')= response.sid
                
